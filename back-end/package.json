{
  "name": "popyns-backend",
  "version": "0.1.0",
  "private": true,
  "description": "POPYNS API — webhook do Clerk + endpoints sensíveis (matching, transições, jobs)",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "dev": "node --watch --env-file=.env src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "@clerk/express": "^1.3.0",
    "@supabase/supabase-js": "^2.45.0",
    "cors": "^2.8.5",
    "express": "^5.0.1",
    "pino": "^9.5.0",
    "pino-http": "^10.3.0",
    "svix": "^1.42.0"
  },
  "devDependencies": {
    "pino-pretty": "^11.3.0"
  }
}
